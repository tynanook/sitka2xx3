Attribute VB_Name = "GlobalPara"
Global sites_tested() As Boolean            'array showing which sites tested in the flow (True = tested)
Global sites_active() As Boolean    'array showing which sites are active(passed) during last test in flow
Global sites_inactive() As Boolean
